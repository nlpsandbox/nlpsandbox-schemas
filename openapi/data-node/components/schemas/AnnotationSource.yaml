type: object
description: The source of an annotation record
properties:
  resourceSource:
    $ref: ../../../commons/components/schemas/ResourceSource.yaml
  # s3Source:
  #   $ref: S3Source.yaml
# minProperties: 1
maxProperties: 1
required:
  - resourceSource