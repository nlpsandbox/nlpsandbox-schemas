type: object
description: A page of note stores
allOf:
  - $ref: ../../../commons/components/schemas/PageResponse.yaml
  - type: object
    properties:
      items:
        description: An array of note stores
        type: array
        items:
          $ref: ../schemas/NoteStore.yaml