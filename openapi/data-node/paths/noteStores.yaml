parameters:
  - in: path
    name: datasetId
    description: The ID of the dataset
    required: true
    schema:
      type: string
post:
  tags:
    - NoteStore
  summary: Create a note store linked to a dataset ID
  description: Create a note store with the name specified
  operationId: "noteStores.create"
  requestBody:
    content:
      application/json:
        schema:
          $ref: ../components/schemas/NoteStore.yaml
  responses:
    '200':
      content:
        application/json:
          schema:
            $ref: ../components/schemas/NoteStore.yaml
      description: Success
    '403':
      $ref: ../../commons/components/responses/Unauthorized.yaml
    '404':
      $ref: ../../commons/components/responses/NotFound.yaml
get:
  tags:
    - NoteStore
  summary: Get all note stores by dataset ID
  description: Returns the note stores for a given dataset ID
  operationId: "noteStores.read_all"
  parameters:
    - in: query
      name: limit
      description: Maximum number of results returned
      required: false
      schema:
        type: integer
        default: 10
        minimum: 10
    - in: query
      name: offset
      description: Index of the first result that must be returned
      required: false
      schema:
        type: integer
        default: 0
        minimum: 0
  responses:
    '200':
      content:
        application/json:
          schema:
            type: object
            properties:
              items:
                type: array
                items:
                  $ref: ../components/schemas/NoteStore.yaml
            allOf:
              - $ref: ../../commons/components/schemas/PageResponse.yaml
      description: Success
    '403':
      $ref: ../../commons/components/responses/Unauthorized.yaml
    '404':
      $ref: ../../commons/components/responses/NotFound.yaml