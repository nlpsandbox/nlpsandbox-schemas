type: object
description: Configuration for deidentifying date entities
anyOf:
  - $ref: ./GenericDeidentification.yaml
  - properties:
      deidentificationType:
        type: string
        enum:
          - date_offset
      offset:
        type: integer
        description: Number of days by which to offset dates in text