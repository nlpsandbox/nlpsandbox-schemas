post:
  tags:
    - Deidentified Notes
  summary: Get deidentified note
  description: Returns the deidentified note
  operationId: "deidentifiedNote.read"
  requestBody:
    content:
      application/json:
        schema:
          $ref: ../components/schemas/DeidentifyRequest.yaml
  responses:
      '200':
        description: Success
        content:
          application/json:
            schema:
              $ref: ../components/responses/DeidentifyResponse.yaml
      '403':
        $ref: ../../commons/components/responses/Unauthorized.yaml