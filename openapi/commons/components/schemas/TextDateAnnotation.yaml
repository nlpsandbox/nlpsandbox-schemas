type: object
description: A date annotation in a text
allOf:
  - $ref: TextAnnotation.yaml
  - type: object
    properties:
      dateFormat:
        description: Date format (ISO 8601)
        type: string
