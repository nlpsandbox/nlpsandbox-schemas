type: object
description: A annotation record
properties:
  name:
    description: Resource name of the annotation
    type: string
    readOnly: true
  annotationSource:
    $ref: AnnotationSource.yaml
  annotationType:
    description: The type of the annotation
    type: string
    enum:
      - text_date
      - text_person_name
      - text_physical_address
  # annotation:
  #   oneOf:
  #     - $ref: TextDateAnnotation.yaml
  #     - $ref: TextPersonNameAnnotation.yaml
  #     - $ref: TextPhysicalAddressAnnotation.yaml
  # discriminator:
  #   propertyName: annotationType
  # textAnnotation:
  #   $ref: TextAnnotation.yaml